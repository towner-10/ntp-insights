{
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "prisma": {
    "schema": "packages/database/prisma/schema.prisma"
  },
  "scripts": {
    "build": "turbo run build --no-daemon",
    "start:backend": "turbo run start --filter=backend --filter=types --continue --no-daemon",
    "dev": "turbo run dev --continue --no-daemon",
    "dev:frontend": "turbo run dev --filter=frontend --filter=types --continue --no-daemon",
    "dev:backend": "turbo run dev --filter=backend --filter=types --continue --no-daemon",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch -- "
  },
  "devDependencies": {
    "prettier": "^2.5.1",
    "tsx": "^3.7.1",
    "turbo": "^1.10.1"
  },
  "packageManager": "yarn@1.22.19"
}
